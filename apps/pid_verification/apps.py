from django.apps import AppConfig


class PidVerificationConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.pid_verification'
    verbose_name = 'P&ID Verification'
