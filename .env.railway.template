# ========================================================================
# RAILWAY PRODUCTION ENVIRONMENT VARIABLES
# ========================================================================
# Copy these to Railway Dashboard → Variables
# Matches Docker container configuration for consistency
# ========================================================================

# Django Settings (matches docker-compose.yml)
DEBUG=False
SECRET_KEY=47++##pw5*gz8%bouu3(=4z@m&hv5gv5sbguvpm=ckz)!vwg9v
ALLOWED_HOSTS=<your-railway-domain>.railway.app,localhost

# Database (Railway PostgreSQL Plugin)
DB_NAME=${{PGDATABASE}}
DB_USER=${{PGUSER}}
DB_PASSWORD=${{PGPASSWORD}}
DB_HOST=${{PGHOST}}
DB_PORT=${{PGPORT}}

# Redis (Railway Redis Plugin)
CELERY_BROKER_URL=${{REDIS_URL}}
CELERY_RESULT_BACKEND=${{REDIS_URL}}

# MongoDB (Railway MongoDB Plugin or External)
MONGODB_URI=mongodb://<user>:<password>@<host>:27017
MONGODB_DATABASE=aiflow

# CORS (update with your Vercel frontend URL)
CORS_ALLOWED_ORIGINS=https://<your-vercel-app>.vercel.app,http://localhost:3000

# OpenAI API (same key as docker-compose)
OPENAI_API_KEY=<your-openai-api-key>

# Gunicorn Settings (matches docker-compose.yml)
WEB_CONCURRENCY=3
GUNICORN_TIMEOUT=600
GUNICORN_WORKERS=3

# AWS S3 (Optional - for production file storage)
USE_S3=False
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=

# ========================================================================
# IMPORTANT: Never commit this file with actual values!
# Add .env.railway to .gitignore
# ========================================================================
